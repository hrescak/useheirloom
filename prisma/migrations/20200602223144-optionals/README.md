# Migration `20200602223144-optionals`

This migration has been generated by Matej Hrescak at 6/2/2020, 10:31:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Recipe" ALTER COLUMN "cookDuration" DROP NOT NULL,
ALTER COLUMN "imageURL" DROP NOT NULL,
ALTER COLUMN "instructions" DROP NOT NULL,
ALTER COLUMN "name" DROP NOT NULL,
ALTER COLUMN "prepDuration" DROP NOT NULL,
ALTER COLUMN "publicID" DROP NOT NULL,
ALTER COLUMN "servings" DROP NOT NULL,
ALTER COLUMN "sourceName" DROP NOT NULL,
ALTER COLUMN "sourceURL" DROP NOT NULL,
ALTER COLUMN "summary" DROP NOT NULL,
ALTER COLUMN "totalDuration" DROP NOT NULL;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200601223157-avataroptional..20200602223144-optionals
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Unit {
   TSP
@@ -54,23 +54,23 @@
 }
 model Recipe {
   id                 Int      @default(autoincrement()) @id
-  name               String
-  summary            String
-  publicID           String   @unique
-  sourceName         String
-  sourceURL          String
-  servings           Int
-  prepDuration       Int
-  cookDuration       Int
-  totalDuration      Int
+  name               String?
+  summary            String?
+  publicID           String?   @unique
+  sourceName         String?
+  sourceURL          String?
+  servings           Int?
+  prepDuration       Int?
+  cookDuration       Int?
+  totalDuration      Int?
   kitchen            Kitchen  @relation(references: [id], fields: [kitchenId])
   kitchenId          Int
-  imageURL           String
+  imageURL           String?
   //ingredients  RecipeIngredient[]
   ingredientSections String[]
-  instructions       String
+  instructions       String?
   isDeleted          Boolean  @default(value: false)
   //categories   Category[] @relation(references: [id])
   starredBy          User[]
```


