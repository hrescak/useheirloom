# Migration `20200601223017-optional-desc`

This migration has been generated by Matej Hrescak at 6/1/2020, 10:30:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Kitchen" ALTER COLUMN "description" DROP NOT NULL;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200601212759-onceagain..20200601223017-optional-desc
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Unit {
   TSP
@@ -29,27 +29,27 @@
   SEASON
 }
 model User {
-  id             Int        @default(autoincrement()) @id
-  email          String     @unique
+  id             Int       @default(autoincrement()) @id
+  email          String    @unique
   name           String
-  googleId       Int? @unique
+  googleId       Int?      @unique
   avatarUrl      String
   hash           String
   salt           String
-  ownKitchen     Kitchen    @relation("KitchenOwnership")
-  kitchens       Kitchen[]  @relation(references: [id])
+  ownKitchen     Kitchen   @relation("KitchenOwnership")
+  kitchens       Kitchen[] @relation(references: [id])
   starredRecipes Recipe[]
 }
 model Kitchen {
-  id          Int      @default(autoincrement()) @id
+  id          Int        @default(autoincrement()) @id
   name        String
-  description String
-  owner       User     @relation("KitchenOwnership", references: [id], fields: [ownerId])
+  description String?
+  owner       User       @relation("KitchenOwnership", references: [id], fields: [ownerId])
   ownerId     Int
-  users       User[]   @relation(references: [id])
+  users       User[]     @relation(references: [id])
   recipes     Recipe[]
   categories  Category[]
 }
@@ -100,8 +100,8 @@
 model Category {
   id          Int          @default(autoincrement()) @id
   name        String
   description String
-  kitchen      Kitchen         @relation(references: [id], fields: [kitchenId])
-  kitchenId    Int
+  kitchen     Kitchen      @relation(references: [id], fields: [kitchenId])
+  kitchenId   Int
   type        CategoryType
 }
```


