# Migration `20200601212759-onceagain`

This migration has been generated by Matej Hrescak at 6/1/2020, 9:27:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Category" DROP CONSTRAINT IF EXiSTS "Category_authorId_fkey",
DROP COLUMN "authorId",
ADD COLUMN "kitchenId" integer  NOT NULL ;

ALTER TABLE "public"."Category" ADD FOREIGN KEY ("kitchenId")REFERENCES "public"."Kitchen"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200601212719-moved_cat..20200601212759-onceagain
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Unit {
   TSP
@@ -39,9 +39,8 @@
   salt           String
   ownKitchen     Kitchen    @relation("KitchenOwnership")
   kitchens       Kitchen[]  @relation(references: [id])
   starredRecipes Recipe[]
-  categories     Category[]
 }
 model Kitchen {
   id          Int      @default(autoincrement()) @id
@@ -50,8 +49,9 @@
   owner       User     @relation("KitchenOwnership", references: [id], fields: [ownerId])
   ownerId     Int
   users       User[]   @relation(references: [id])
   recipes     Recipe[]
+  categories  Category[]
 }
 model Recipe {
   id                 Int      @default(autoincrement()) @id
@@ -100,8 +100,8 @@
 model Category {
   id          Int          @default(autoincrement()) @id
   name        String
   description String
-  author      User         @relation(references: [id], fields: [authorId])
-  authorId    Int
+  kitchen      Kitchen         @relation(references: [id], fields: [kitchenId])
+  kitchenId    Int
   type        CategoryType
 }
```


